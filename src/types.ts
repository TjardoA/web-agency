export type Service = {
  title: string;
  description: string;
};

export type Project = {
  title: string;
  tag: string;
};

export type PortfolioItem = {
  title: string;
  summary: string;
  images: string[];
};

export type Testimonial = {
  name: string;
  role: string;
  company: string;
  quote: string;
};
